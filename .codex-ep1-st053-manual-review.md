Status: resolved

P0/P1/P2 findings:

- P0: None.
- P1: A timed-out primary plan can invoke Terra fallback if its partial output contains “at capacity.” The capacity check runs before `error.timedOut` is handled, violating the requirement that timeouts never fall back.
- P2: No runner-level automated coverage exercises the fallback decision matrix, so this timeout regression is not protected by the passing general checks.

Scope verdict: pass — all four changed paths versus `dev` are allowed; no forbidden-path changes detected.

Remaining risks: Capacity detection remains text-pattern dependent; provider wording changes may prevent a legitimate fallback.
